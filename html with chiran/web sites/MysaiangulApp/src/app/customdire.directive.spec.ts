import { CustomdireDirective } from './customdire.directive';

describe('CustomdireDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdireDirective();
    expect(directive).toBeTruthy();
  });
});
