import { CustomdirectivesDirective } from './customdirectives.directive';

describe('CustomdirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
